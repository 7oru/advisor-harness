# Executor Prompt Notes

The harness builds the final executor prompt at runtime. The security questionnaire workflow adds context requiring:

- customer-readable answer drafts
- evidence references
- risk flags
- open questions
- advisor requests for high-risk or unsupported claims
- memory proposals only when reusable facts have concrete source excerpts
